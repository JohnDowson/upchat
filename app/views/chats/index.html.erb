<%= render partial: "/nav", locals: { text: "Upchats" } %>

<ul class="list-group" id="chats">
  <% @chats.each do |chat| %>
    <li class="list-group-item d-flex justify-content-between align-items-start">
      <span><%= render chat %></span>
      <span><%= link_to "Show upchat", chat %></span>
    </li>
  <% end %>
</div>
